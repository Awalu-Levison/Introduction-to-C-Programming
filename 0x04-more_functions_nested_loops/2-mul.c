#include "main.h"
/**
 * mul - Multiplies two integers
 * @a:First parameter to use
 * @b:Second parameter to use
 * Return: 0 on success
 */
int mul(int a, int b)
{
	int product = (a * b);

	return (product);
	return (0);
}
