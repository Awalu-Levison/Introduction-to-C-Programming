#include "main.h"
/**
 * _isupper - Checking for uppercases in a program
 * @c:Parameter to use
 * Return: (0 on success)
 */
int _isupper(int c)
{
	if (c == '97'; c <= '122'; c++)
	{
		return (1);
	}

	else
	{
		return (0);
	}

	return (0);
}
